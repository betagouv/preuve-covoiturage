name: 'Release'

on:
  push:
    branches: [dev]

jobs:
  api-build:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Semantic Release
        uses: cycjimmy/semantic-release-action@v3
        with:
          branches: |
            [
              'dev',
            ]
        env:
          GITHUB_TOKEN: ${{ secrets.PERSONAL_ACESS_TOKEN_RELEASE }}