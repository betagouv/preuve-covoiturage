export interface RetributionInterface {
  free: number;
  min: number;
  max: number;
  perPassenger: [false];
  perKmForDriver: [false];
  perKmForPassenger: [false];
  valueForDriver: number;
  valueForPassenger: number;
}
