[project]
name = "rpc-dbt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"

dependencies = [
    "dbt-core>=1.11",
    "dbt-postgres>=1.10",
    "dbt-osmosis>=1.3",
    "boto3>=1.40.76",
    "python-dotenv>=1.2.1",
    "requests>=2.32.5",
    "duckdb>=1.5.0",
    "typer>=0.24.1",
    "psycopg[binary]>=3.2",
    "pyyaml>=6",
    "redis>=6",
]

[dependency-groups]
dev = [
    "sqlfluff>=4.2.1",
    "sqlfluff-templater-dbt>=4.2.1",
    "pytest>=8",
]
