export type ResultInterface = void;

export const handlerConfig = {
  service: 'trip',
  method: 'activeCampaignExcelExport',
};

export const signature = `${handlerConfig.service}:${handlerConfig.method}`;
