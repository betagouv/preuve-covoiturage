import test from 'ava';



test('BuildExcelExportAction: should create xlsx file for last month if no date range provided', async (t) => {
  // Arrange
 //  tripRepositoryProviderStub = sinon.stub(tripRepositoryProvider, 'searchWithCursorForCampaign');
  // tripRepositoryProviderStub.throws(NotFoundException)

  // Act 
  // buildExcel.call(ACTIVE_CAMPAIGN_ID)

  // Assert
})