import * as ajv from './ajv';
import * as campaign from './campaign';
import * as campaignMetadata from './campaignMetadata';
import * as connections from './connections';
import * as migration from './migration';

export const config = {
  ajv,
  campaign,
  campaignMetadata,
  connections,
  migration,
};
