import { Timezone } from "@/pdc/providers/validator/types.ts";

/**
 * Default timezone used in the application
 *
 * Check helpers/dates.helper.ts for date and time tools
 */
export const defaultTimezone: Timezone = "Europe/Paris";
