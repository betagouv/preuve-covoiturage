import seeder from './seeder';
import aom from './schemas/aom';
import identity from './schemas/identity';
import position from './schemas/position';
import rank from './schemas/rank';

const schemas = {
  aom,
  identity,
  position,
  rank,
};

export {
  seeder,
  schemas,
};
